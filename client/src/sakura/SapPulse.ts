import * as THREE from 'three'

/** Stub — SapPulse system placeholder. */
export class SapPulseSystem {
  constructor(_scene: THREE.Scene) {}
  createPulse(_positions: THREE.Vector3[], _color: number | THREE.Color) {}
  tick(_dt: number) {}
  dispose() {}
}
